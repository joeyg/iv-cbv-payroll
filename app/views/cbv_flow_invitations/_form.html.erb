<%= form_with(model: cbv_flow_invitation) do |form| %>
  <% if cbv_flow_invitation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cbv_flow_invitation.errors.count, "error") %> prohibited this cbv_flow_invitation from being saved:</h2>

      <ul>
        <% cbv_flow_invitation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
